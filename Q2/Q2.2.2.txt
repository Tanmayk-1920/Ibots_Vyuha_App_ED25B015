The program crashes at the fourth iteration in the line [yellow = malloc(16)]{first line of the loop}

The system runs out of contiguous space to allocate 16 bytes for yellow. Therefore, malloc(16) returns NULL. If the program attempts to write to yellow right after, it will crash due to a null pointer dereference.